# Use exact versions for reproducible builds
save-exact=true

# Use package-lock.json
package-lock=true

# Set registry (optional, can be removed if using default)
# registry=https://registry.npmjs.org/
